training:
  epochs: 5
  batch_size: 16
  learning_rate: 3.0e-4
  weight_decay: 0.01
  grad_clip_norm: 1.0
  log_interval: 20
  eval_interval: 1
  save_interval: 1

optimizer:
  name: adamw

scheduler:
  name: cosine
  warmup_epochs: 1
